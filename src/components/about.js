export function initAbout() {
  const aboutSection = document.querySelector('#about')

  if (!aboutSection) return

  // Future enhancement: load dynamic content or animate on scroll
  console.log('✓ About section initialized')
}
